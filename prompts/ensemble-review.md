---
description: Run an ensemble review with independent reviewers, then synthesize the findings
argument-hint: "<review request>"
---

Use the `ensemble-review` skill for this review request:

```
$@
```

Load the skill, run the independent reviews, then return the synthesized report.
