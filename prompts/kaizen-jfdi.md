---
description: Capture and immediately resolve workflow or tooling friction as a docs/kaizen note
argument-hint: "[problem context or note path]"
---

Use the `kaizen-jfdi` skill to capture and immediately resolve this kaizen issue:

```
$@
```

If a specific note is provided, resolve it to one file under `docs/kaizen/` and run the fix workflow. If context describes a workflow/tooling problem, capture the note first, then investigate and apply an obvious low-risk fix when available.
